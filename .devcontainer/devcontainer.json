{
  "name": "SequAna Course (ONT)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",

  "features": {
    "ghcr.io/devcontainers/features/conda:1": {}
  },

  "postCreateCommand": "conda install -n base -y -c conda-forge mamba && mamba env update -f environment.yml --prune && echo 'conda activate ONT26' >> ~/.bashrc"
}
