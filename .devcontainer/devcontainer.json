{
  "name": "SequAna Course (ONT)",
  // Pin to jammy (22.04) to resolve the Docker-in-Docker compatibility issue
  "image": "mcr.microsoft.com/devcontainers/base:jammy",

  "features": {
    "ghcr.io/devcontainers/features/conda:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:1": {
      "version": "latest",
      "moby": true
    }
  },

  "postCreateCommand": "conda init && conda install -n base -y conda-forge::mamba && mamba shell init --shell bash && mamba create -n ONT26 -y -c conda-forge -c bioconda python busco && echo 'conda activate ONT26' >> ~/.bashrc"
}
