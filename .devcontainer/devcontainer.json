{
  "name": "SequAna Course (ONT)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",

  "features": {
    "ghcr.io/devcontainers/features/conda:1": {}
  },

  "postCreateCommand": "conda install -n base -y -c conda-forge mamba && mamba env create -f environment.yml && echo 'conda activate ONT26' >> ~/.bashrc"
}
