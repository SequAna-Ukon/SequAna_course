{
  "name": "SequAna Course (ONT)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",

    "features": {
    "ghcr.io/devcontainers/features/conda:1": {},
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
  },

  "postCreateCommand": "conda init && conda install -n base -y conda-forge::mamba && mamba shell init --shell bash && mamba create -n ONT26 -y -c conda-forge -c bioconda python busco && echo 'conda activate ONT26' >> ~/.bashrc && mamba create -n EDTA -y -c conda-forge -c bioconda edta"
}
