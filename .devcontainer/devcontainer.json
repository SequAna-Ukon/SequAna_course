{
  "name": "SequAna Course (ONT)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "features": {
    "ghcr.io/devcontainers/features/conda:1": {
      "version": "latest"
    }
  },

  "postCreateCommand": "conda init && conda install -n base -y conda-forge::mamba && mamba create -n ONT26 -y -c conda-forge -c bioconda python busco && echo 'mamba activate ONT26' >> ~/.bashrc"
}
